<div class="section s-white">
  <% if !user_signed_in? %>
  <div class="section-title">
    Echanger
  </div>
  <div class="section-description">
    Tout au long de l'année, Agroé organise des rencontres, des visites et des webinaires pour que les membres puissent échanger, écouter des experts et aller à la rencontre de leurs parties prenantes.
  </div>
  <% end %>
  <div class="events-list-content">
    <% @events.where('date >= ?', Date.today.to_s).where('event_type != ?', 'contenu').order(:date).each do |e| %>
      <%= render 'shared/events/event_listing_card', event: e %>
    <% end %>
  </div>
</div>
