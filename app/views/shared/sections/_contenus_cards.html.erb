<div class="section s-grey">
  <% if !user_signed_in? %>
  <div class="section-description">
    L'ensemble des présentations, compte-rendus, photos, articles sont triés par thématiques et mis à disposition des adhérents. Les adhérents contribuent aussi à alimenter ces dossiers thématiques.
  </div>
  <% else %>
  <% end %>
  <div class="cards-content">
    <div class="row">
    <% @events.where('event_type = ?', 'contenu').order(:date).each do |e| %>
      <%= render 'shared/events/event_card', event: e %>
    <% end %>
    </div>
  </div>
  <div class="lock-information">
    <i class="fas fa-lock"></i> : Contenu réservé aux adhérents "Agroé et "AgroclubRSE"
  </div>
</div>
