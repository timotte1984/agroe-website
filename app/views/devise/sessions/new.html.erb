
<div class="devise-form-container">
  <div class="section-title">
    Connexion à l'espace adhérent
  </div>
  <%= link_to "Devenir adhérent ?", adhesion_path %>

<div class="device-form">
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-inputs">
    <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
    <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
    <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Log in" , class: "btn btn-success"%>
  </div>
    <% end %>

<br/>
<br/>

   <div class="experts"> <strong>Vous ne savez plus si vous êtes adhérent ?</strong>
   <br/> <br/> Envoyez un petit email à  <a href="mailto:j.mottet@agroe.fr">j.mottet@agroe.fr</a>. <br/>Il se fera un plaisir de vous répondre <br/> et vous fournira par la même occasion un mot de passe <br/> pour accéder à l'espace adhérent.
   </div>
</div>

